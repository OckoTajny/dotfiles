# The shell (quickshell) has segfaulted inside Mesa and taken the whole daemon
# down with exit status 0, which Restart=on-failure ignores. Always bring it
# back, otherwise the bar and dock just disappear.
[Service]
Restart=always
RestartSec=3
