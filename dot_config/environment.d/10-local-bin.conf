# ~/.local/bin first so niri's spawn-at-startup "ambxst" (and anything else the
# systemd user session spawns) hits the single-instance guard wrapper instead
# of /usr/local/bin/ambxst. Two shells = double exclusive zone = gap under bar.
PATH=$HOME/.local/bin:$PATH
